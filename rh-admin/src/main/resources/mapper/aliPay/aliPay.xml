<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ntnikka.modules.pay.aliPay.dao.AliOrderDao">
	
	<select id="checkRepeatId" parameterType="java.lang.Long" resultType="java.lang.Integer">
		SELECT count(1)
		from trade_order
		where order_id = #{orderId}
	</select>

	<insert id="savetradePrecreateMsg" parameterType="com.ntnikka.modules.pay.aliPay.entity.TradePrecreateMsg">
		INSERT  into  trade_precreate_msg(order_id , code , msg )
		values (#{tradePrecreateMsg.orderId},#{tradePrecreateMsg.code},#{tradePrecreateMsg.msg})
	</insert>

</mapper>